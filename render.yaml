services:
  - type: web
    name: portfolio-analytics-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn multi-asset-portfolio-dash.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
